// Starter file from `init` — replace with your own code.
// Package entry point: re-exports the demo component so the built package has a
// public surface to publish.
export { MathEngine } from "./math-engine/MathEngine.js";
export { MathError } from "./math-engine/MathError.js";
export type { MathResult } from "./math-engine/MathResult.js";
export type { Operation } from "./math-engine/Operation.js";
